import * as React from "react";
import TextArea from "./index";
import { strings } from "../../variables/constants";

const textareaCosmos = () => {
  return <TextArea ph={strings.ADD_CARD_PLACEHOLDER} />;
};

export default textareaCosmos;
